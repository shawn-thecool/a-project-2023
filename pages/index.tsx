import { useQuery } from '@tanstack/react-query'
import Head from 'next/head'
import SimpleLayout from '../src/gg/adaptor/web/assets/layouts/SimpleLayout'
import { UserUseCase } from '../src/gg/application/usecases/User.uc'
import UserEntity from '../src/gg/domain/entities/User.entity'

function ContainerRC() {
  // case 1 use-query

  const userId = '111'
  const siteId = '222'

  const uc = new UserUseCase()
  const { data: user } = useQuery<UserEntity, Error>([`/users/${userId}`, () => uc.getUser(userId)])
  const { data: site } = useQuery({ queryKey: ['site', siteId], queryFn: () => uc.getSite(siteId) })
  console.log(user?.id.value)
  console.log(site?.id.value)
  console.log(uc.canCreateSite(userId))

  return <></>
}

export default function Home() {
  return (
    <>
      <ContainerRC />
      <Head>
        <title>Create Next App</title>
        <meta name='description' content='Generated by create next app' />
        <link rel='icon' href='/favicon.ico' />
      </Head>

      <SimpleLayout head={'index.page'} main={<div>index</div>} />
    </>
  )
}
